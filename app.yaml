runtime: nodejs14

handlers:
  - url: /(.*\.(js|css|png|jpg|gif|ico|json|svg))$
    static_files: build/\1
    upload: build/.*\.(js|css|png|jpg|gif|ico|json|svg)$

  - url: /.*
    static_files: build/index.html
    upload: build/index.html
